/**
 * Shared API contracts will be introduced only after the first compatible
 * extraction of the web and API applications. Keeping this package explicit
 * prevents cross-app imports from becoming accidental filesystem imports.
 */
export {}
